const RANDOM_MINIATURES_COUNT = 10;

const DEBOUNCE_TIMEOUT = 500;

const Filter = {
  DEFAULT: 'filter-default',
  RANDOM: 'filter-random',
  DISCUSSED: 'filter-discussed',
};

export { RANDOM_MINIATURES_COUNT, DEBOUNCE_TIMEOUT, Filter };
