const uploadElement = document.querySelector('.img-upload__start input[type=file]');
const previewElement = document.querySelector('.img-upload__preview img');
const effectElement = document.querySelectorAll('.effects__preview');

export { uploadElement, previewElement, effectElement };
